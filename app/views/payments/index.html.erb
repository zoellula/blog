<table>
	<tr>
		<th>Name</th>
	    <th>Email</th>
	    <th colspan="3">Action</th>
	</tr>

	<% @users.each do |u| %>
	<tr>
	    <td><%= u.full_name %></td>
	    <td><%= u.email %></td>
	</tr>
	<% end %>
</table>
